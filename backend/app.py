from app_function_call import clean_data, run_streamlit_app

# Optionally run data cleaning first (comment out if not needed)
# clean_data()

# Run the Streamlit app
if __name__ == "__main__":
    run_streamlit_app()
